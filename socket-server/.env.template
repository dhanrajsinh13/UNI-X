# Socket Server Environment Variables
# This file should mirror critical settings from the main app's .env.local
# CRITICAL: JWT_SECRET must match EXACTLY between main app and socket server

# ===========================================
# AUTHENTICATION - MUST MATCH MAIN APP
# ===========================================

# JWT Secret Key - CRITICAL: Must match main app EXACTLY
# Use the same value from your main .env.local file
JWT_SECRET=your-super-secret-jwt-key-change-this

# ===========================================
# SERVER CONFIGURATION
# ===========================================

# Port for socket server
PORT=3001

# Node Environment
NODE_ENV=development

# ===========================================
# CORS CONFIGURATION
# ===========================================

# CORS Origin (main app URL)
# Development: http://localhost:3000
# Production: https://your-app.vercel.app
CORS_ORIGIN=http://localhost:3000

# ===========================================
# DATABASE (if socket server needs direct DB access)
# ===========================================

# MongoDB Connection URI (optional - use if socket server needs DB access)
MONGODB_URI=

# ===========================================
# DEVELOPMENT/DEBUG
# ===========================================

# Enable debug logging
DEBUG=false
